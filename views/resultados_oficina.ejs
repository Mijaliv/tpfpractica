<!DOCTYPE html>
<html>

<%- include('./head.ejs') %>

    <body>
        <h1>
            <%= title %>
        </h1>
        <ul>
            <% oficinas.forEach(element => { %>
                <li>
                    <%= element.denominacion  %>
                </li>
                <% }) %>
        </ul>
    </body>